[project]
name = "comfyui-krea2-ostris-edit" # Unique identifier for your node. Immutable after creation.
description = "Krea 2 edit (reference image) nodes for LoRAs trained with ai-toolkit. Encodes prompts + reference images through the Krea 2 Qwen3-VL text encoder and patches the model to consume reference latents, with optional KV-cache conditioning."
version = "1.0.1" # Custom Node version. Must be semantically versioned.
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/ostris/ComfyUI-Krea2-Ostris-Edit"

[tool.comfy]
PublisherId = "ostris"
DisplayName = "Krea 2 Ostris Edit"
Icon = "https://ostris.com/wp-content/uploads/2026/07/004.jpg"